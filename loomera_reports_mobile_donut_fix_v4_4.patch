--- a/apps/dashboards/reports_components.py
+++ b/apps/dashboards/reports_components.py
@@ -501,6 +501,29 @@
     }
 
 
+def _decorate_ranking_donut(items):
+    palette = [
+        "#735cbe",
+        "#0ea5e9",
+        "#10b981",
+        "#f59e0b",
+        "#f43f5e",
+    ]
+    total = sum(max(int(item.get("chart_value") or 0), 0) for item in items)
+    cursor = 0.0
+
+    for index, item in enumerate(items):
+        value = max(int(item.get("chart_value") or 0), 0)
+        share = (value / total * 100) if total else 0
+        item["chart_color"] = palette[index % len(palette)]
+        item["chart_start"] = round(cursor, 3)
+        cursor += share
+        item["chart_end"] = round(cursor, 3)
+        item["chart_share_label"] = _percent(share)
+
+    return items
+
+
 def _build_top_services(filtered_qs):
     rows = list(
         filtered_qs.values("service__service_name")
@@ -521,6 +544,7 @@
         }
         for row in rows
     ]
+    items = _decorate_ranking_donut(items)
     return {"items": items, "is_empty": len(items) == 0}
 
 
@@ -547,6 +571,7 @@
                 "chart_value": int(row["revenue"] or 0),
             }
         )
+    items = _decorate_ranking_donut(items)
     return {"items": items, "is_empty": len(items) == 0}
 
 
--- a/templates/dashboards/partials/reports_ranking_chart.html
+++ b/templates/dashboards/partials/reports_ranking_chart.html
@@ -1,30 +1,27 @@
 {% if widget.is_empty %}
   {% include "dashboards/components/empty_state.html" with icon=empty_icon title=empty_title description=empty_description %}
 {% else %}
-  <div class="space-y-3" data-reports-ranking-chart>
-    <div class="mx-auto w-full max-w-[240px]">
-      <canvas data-reports-ranking-canvas
-              style="display:block;width:100%;height:220px;"
-              role="img"
-              aria-label="{{ title }}"></canvas>
-    </div>
-
-    <div hidden>
-      {% for item in widget.items %}
-        <span data-report-ranking-point
-              data-label="{{ item.title|escape }}"
-              data-meta="{{ item.meta|escape }}"
-              data-value="{{ item.chart_value|default:0 }}"
-              data-value-label="{{ item.value|escape }}"></span>
-      {% endfor %}
+  <div class="space-y-4" data-reports-ranking-chart>
+    <div class="mx-auto flex w-full max-w-[250px] items-center justify-center py-2">
+      <div class="relative h-40 w-40 shrink-0 rounded-full"
+           style="background: conic-gradient({% for item in widget.items %}{{ item.chart_color }} {{ item.chart_start }}% {{ item.chart_end }}%{% if not forloop.last %}, {% endif %}{% endfor %});"
+           role="img"
+           aria-label="{{ title }}">
+        <div class="absolute inset-[20%] flex flex-col items-center justify-center rounded-full bg-white text-center shadow-[inset_0_0_0_1px_rgba(115,92,190,0.06)]">
+          <strong class="text-xl font-black text-loomera-textPrimary">{{ widget.items|length }}</strong>
+          <span class="mt-0.5 text-[9px] font-bold text-loomera-textMuted">مورد برتر</span>
+        </div>
+      </div>
     </div>
 
     <div class="grid grid-cols-1 gap-2 sm:grid-cols-2">
       {% for item in widget.items %}
-        <div class="rounded-[14px] bg-loomera-bgSubtle px-2.5 py-2" data-report-ranking-legend-item>
+        <div class="rounded-[14px] bg-loomera-bgSubtle px-2.5 py-2">
           <div class="flex items-center gap-1.5">
-            <span class="h-2 w-2 shrink-0 rounded-full" data-report-ranking-legend-color></span>
+            <span class="h-2.5 w-2.5 shrink-0 rounded-full"
+                  style="background-color: {{ item.chart_color }};"></span>
             <span class="min-w-0 flex-1 truncate text-[9px] font-black text-loomera-textSecondary">{{ item.title }}</span>
+            <span class="shrink-0 text-[9px] font-black text-loomera-textMuted">{{ item.chart_share_label }}</span>
           </div>
           <div class="mt-1 flex items-end justify-between gap-2">
             <strong class="truncate text-[10px] font-black text-loomera-textPrimary">{{ item.value }}</strong>
